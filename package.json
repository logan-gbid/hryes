{
  "name": "hryes-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "HRYES desktop clients and shared resume analysis server.",
  "scripts": {
    "server": "npm --prefix server start",
    "client:v1.0": "npm --prefix versions/v1.0/client start",
    "client:v1.0:build": "npm --prefix versions/v1.0/client run build",
    "client:v1.1": "npm --prefix versions/v1.1/client run dev",
    "client:v1.1:build": "npm --prefix versions/v1.1/client run build",
    "install:all": "npm --prefix server install && npm --prefix versions/v1.0/client install && npm --prefix versions/v1.1/client install"
  }
}
